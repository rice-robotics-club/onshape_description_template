{
  "robot_name": "robot", # TODO: replace with robot name
  "package_name": "onshape_description_template", # TODO: replace with package name
  "assets_directory": "meshes",
  "output_filename": "urdf/robot",
  "output_format": "urdf",
  "document_id": "", # TODO: replace with document_id (after d/ in url)
  "workspace_id": "", # TODO: replace with workspace_id (after w/ in url)
  "element_id": "", # TODO: replace with element_id (after e/ in url)
  "assembly_name": "" # TODO: replace with OnShape assembly name
}
